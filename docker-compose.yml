version: "3.9"

services:
  web:
    build: .
    container_name: site_audit_app
    ports:
      - "8000:8000"
    volumes:
      - ./reports:/app/reports
      - ./site_audit.db:/app/site_audit.db
    env_file:
      - .env
    restart: unless-stopped